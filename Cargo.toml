[workspace]
members = ["crates/workshop-rs", "crates/workshop-rs-cli"]
resolver = "3"

[workspace.package]
version = "0.1.4"
edition = "2024"
rust-version = "1.85.0"
license = "MIT"
repository = "https://github.com/wrightkit/workshop-rs"
description = "The canonical multi-locale Overwatch Workshop semantic core."

[workspace.lints.rust]
# Unsafe operations inside an unsafe function still need an explicit block so
# the unsafe surface and its invariants remain visible at the operation site.
unsafe_op_in_unsafe_fn = "deny"
# Public items in a private module are not part of a usable crate API.
unreachable_pub = "deny"

[workspace.lints.clippy]
# These groups cover bug-prone findings without enabling broad style or
# restriction policies; CI promotes the remaining warning-level checks.
correctness = { level = "deny", priority = -1 }
suspicious = { level = "deny", priority = -1 }
perf = { level = "warn", priority = -1 }
# These development/progress macros must not reach shipped code.
dbg_macro = "deny"
todo = "deny"
unimplemented = "deny"

[profile.dist]
inherits = "release"
lto = "thin"
