name: Release-plz

on:
  push:
    branches: [main]

jobs:
  release-plz:
    uses: wrightkit/.github/.github/workflows/release-plz-crates.yml@19ea0a318d6a1ef626321373f87ff8d59b5a46ae
    permissions:
      contents: write
      pull-requests: write
    secrets:
      github-token: ${{ secrets.GH_TOKEN }}
      cargo-registry-token: ${{ secrets.CARGO_REGISTRY_TOKEN }}
