//! Supported crate-root entry point for Workshop locale detection.
//!
//! The implementation is owned by [`crate::catalog::detect`]; this module
//! keeps detection discoverable alongside the crate's other public operations.

pub use crate::catalog::detect::*;
